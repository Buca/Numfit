class Curve extends Evaluator {

	getClosestX0( x )

	getClosestX1( x )

	constructor( positions, values, type = Linear, fillEdges = 0 ) {};

	update() {};

	evaluate( position ) {};

	set( position, values ) {};

	unset( position ) {};

};